declare interface PrefabBase {
/**
* ```
(get) name: string
(set) (Not Available)
```

Specifies the unique prefab name. The prefab name can be retrieved from within mesh.
**See Also**: `PrefabsModule.get`.
*/
name: string;
}
