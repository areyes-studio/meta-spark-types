/** 
 * The `State` enum describes the state of a GalleryTexture.
 * @property AVAILABLE Indicates that the texture was picked and is available now.
 * @property PENDING Indicates that the texture selection is pending.
 */ 
declare interface State{ 
AVAILABLE, 
PENDING, 
} 
